# DNS-Verifier
Chrome extension that automates DNS and email authentication checks (SPF, DKIM, DMARC) for Klaviyo's Support and CNX teams. Detects sending infrastructure, flags misconfigurations, and generates escalation-ready summaries.
